# CommForge 界面与交互审计

## 审计范围

本轮为 UX 与可访问性联合审计，覆盖总览、通信管理、报文规则、接收规则、自动化任务、运行监控、收发日志和设置 8 个页面。用户目标是快速扫描表格、明确识别状态、完整看到按钮，并能真正完成规则编辑和自动化任务操作。

## 步骤与健康状态

1. 总览与公共表格——健康
   - 用户截图显示表头左对齐，而状态胶囊居中，形成“状态像对不上列”的错觉。
   - 最终所有表头统一居中；名称和长文本保持左对齐，状态与数值居中。
2. 通信管理操作——健康
   - 用户截图中的行内“启动 / 编辑”按钮下边缘被裁切。
   - 最终表格行高、按钮高度和操作列宽度统一；按钮文本和四边边框完整可见。
3. 接收规则浏览与编辑——健康
   - 原页面将规则列表、字段表和测试输入长期并列，左侧窄、右侧空，任务层级混乱。
   - 最终主页面只展示规则清单；编辑器将规则配置、解析字段和匹配测试分为三栏，测试成功状态与解析结果同时可见。
4. 自动化任务管理——健康
   - 原页面字段过长、状态列狭窄，工具栏按钮点击后没有效果。
   - 根因是任务编号放在单元格组件列，读取选中项时得到空值。现已改为从任务名称列读取编号，并提供行内启停、执行、编辑和明确反馈。
5. 字体、颜色与状态——健康
   - 主题改用更清晰的深蓝灰文字、淡蓝灰页面、紫色主操作和语义状态色；四张指标卡使用不同强调色与真实系统图标。

## 优点

- 列表页采用一致的信息架构、表头、行高、隔行底色和选中态。
- 接收规则与报文规则都遵循“列表 → 详细编辑器”，降低主页面密度。
- 自动化任务把来源、规则链路、目标和运行状态放在同一行，可直接解释任务会在何时向哪里发送什么。
- 操作失败或未选择数据时不再静默，页面会给出明确提示。

## 可访问性风险与证据限制

- 截图和 Qt 控件检查可确认按钮尺寸、颜色对比、选中态和状态文本，但不能完整证明屏幕阅读器名称、朗读顺序或复杂键盘导航。
- 1920 与 1440 宽度均已检查；更窄于应用 1120 px 最小窗口宽度的布局不在桌面软件支持范围内。
- 真实报文发送在视觉回归中被显式禁用，自动化执行按钮使用本地模拟成功回执验证事件连接，避免测试连接到网络或串口。

## 证据

- 总览与通信：`docs/images/qa-dashboard.png`、`docs/images/qa-communications.png`。
- 接收规则与自动化：`docs/images/qa-receive-rules-wide.png`、`docs/images/qa-automation-wide.png`。
- 接收规则编辑与成功测试：`docs/images/qa-receive-editor-tested.png`。
- 自动化动态配置窗口：`docs/images/qa-automation-editor.png`。
- 完整技术检查：`design-qa.md`。
